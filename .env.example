# Proshopp Environment Variables
# Copy this file to .env and fill in your actual values

# ====================================
# APPLICATION SETTINGS
# ====================================
NEXT_PUBLIC_APP_NAME="Proshopp"
NEXT_PUBLIC_APP_DESCRIPTION="A modern ecommerce store built with Next.js"
NEXT_PUBLIC_SERVER_URL="http://localhost:3000"

# ====================================
# DATABASE (Neon PostgreSQL)
# ====================================
# Get your database URL from: https://neon.tech
# Format: postgres://username:password@host/database?sslmode=require
DATABASE_URL="postgres://username:password@host/database?sslmode=require"

# ====================================
# AUTHENTICATION (Auth.js v5)
# ====================================
# Generate a secret: npx auth secret
# The command will create .env.local - copy AUTH_SECRET to your .env file
AUTH_SECRET="your-auth-secret-here-generated-by-npx-auth-secret"

# Google OAuth (Optional)
# Get credentials from: https://console.cloud.google.com/apis/credentials
# Create OAuth 2.0 Client ID -> Web application
# Authorized redirect URIs: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# GitHub OAuth (Optional)
# Get credentials from: https://github.com/settings/developers
# Create OAuth App -> Authorization callback URL: http://localhost:3000/api/auth/callback/github
GITHUB_CLIENT_ID="your-github-client-id"
GITHUB_CLIENT_SECRET="your-github-client-secret"

# ====================================
# PAYMENT (Stripe)
# ====================================
# Get your keys from: https://dashboard.stripe.com/apikeys
# Use test mode keys for development
STRIPE_SECRET_KEY="sk_test_your_stripe_secret_key"
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_your_stripe_publishable_key"

# Stripe Webhook Secret
# ⚠️ IMPORTANT: This project uses NGROK for webhook forwarding
# Ngrok URL: https://uninflicted-harper-savable.ngrok-free.dev
#
# Setup:
# 1. Start ngrok: ngrok http 3000
# 2. Go to: https://dashboard.stripe.com/test/webhooks
# 3. Add endpoint: https://uninflicted-harper-savable.ngrok-free.dev/api/webhooks/stripe
# 4. Select events: payment_intent.succeeded, payment_intent.payment_failed, checkout.session.completed
# 5. Copy the webhook signing secret below
# See CLAUDE.md → "Ngrok Setup for Stripe Webhooks" for full documentation
STRIPE_WEBHOOK_SECRET="whsec_your_webhook_secret_from_stripe_dashboard"

# ====================================
# FILE UPLOAD (UploadThing)
# ====================================
# Get your keys from: https://uploadthing.com/dashboard
UPLOADTHING_SECRET="your_uploadthing_secret"
UPLOADTHING_APP_ID="your_uploadthing_app_id"

# ====================================
# EMAIL (Resend)
# ====================================
# Get your API key from: https://resend.com/api-keys
RESEND_API_KEY="re_your_resend_api_key"

# ====================================
# DEVELOPMENT NOTES
# ====================================
# 1. Never commit your .env file to git
# 2. Always use test/development keys in development
# 3. Store production keys securely (use Vercel environment variables)
# 4. Run `npx prisma migrate dev` after setting DATABASE_URL
# 5. Run `npx auth secret` to generate AUTH_SECRET
